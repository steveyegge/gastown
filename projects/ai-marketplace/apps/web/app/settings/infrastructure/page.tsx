import { SettingsPageContent } from "../_components/settings-page"

export const metadata = { title: "Infrastructure Settings — AI Marketplace" }

export default function InfrastructurePage() {
  return <SettingsPageContent category="infrastructure" />
}
