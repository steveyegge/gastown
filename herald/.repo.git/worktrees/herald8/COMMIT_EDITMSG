feat: REST API conversation and status endpoints (he-m17)

Add four new API endpoints behind API key auth:
- GET /api/status: daemon health, registered executors, active channels
- POST /api/chat: send message through agent pipeline, get response
- GET /api/memory/blocks: read pinned memory blocks
- GET /api/tools: list available tools with descriptions

New files:
- internal/api/handlers.go: endpoint handlers with provider interfaces
- internal/api/handlers_test.go: 13 tests covering all endpoints

Changes:
- internal/api/server.go: NewServer accepts optional *Handlers, mounts
  API routes alongside gateway routes behind shared auth
- internal/daemon/daemon.go: wire coordinator, block store, and handler
  func into API handlers via adapter types; always start API server
