feat: memory hooks: post-response fact/goal extraction goroutine (hr-31j.5)

Background goroutine calls LLM after each turn to extract facts, goals,
and insights. Stores to STM with embeddings. Triggers block reflection
when STM reaches 50 items. Wired into daemon handler after agent response.
