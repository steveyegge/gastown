feat: add prerequisite checks and herald setup subcommand (hr-fye.1)

Create internal/setup/prerequisites.go with checks for:
- PostgreSQL connectivity (suggests docker-compose up if unreachable)
- Auto-run database migrations via existing db/migrate runner
- LLM API keys (requires at least one of ANTHROPIC/OPENAI/GEMINI)
- Ollama for local embeddings (optional, shown as SKIP if absent)

Wire into 'herald setup' subcommand in cmd/herald/main.go.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
