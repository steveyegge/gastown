feat: add channel configuration wizard for setup CLI (hr-fye.3)

Add internal/setup/channels.go with interactive wizard that walks users
through enabling channels (SMS, Slack, Telegram, WhatsApp, iMessage, Voice)
and collecting credentials. Validates credential formats where possible.

Add config.Save() to persist configuration to YAML. Integrate channel
wizard into the setup command after prerequisite checks.

Includes comprehensive tests for all channels, validation, and edge cases.
