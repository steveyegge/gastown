feat: add prometheus metrics for observability (he-8zw)

Add internal/metrics package with prometheus instrumentation:
- HTTP request duration/count histograms per endpoint
- LLM request duration/count/tokens per provider
- Tool execution duration/count/errors per executor
- Channel message count (inbound/outbound) per channel
- Memory: STM size gauge, LTM search latency histogram
- Runtime goroutines gauge, DB connection pool stats collector
- /metrics endpoint exposed on API server (unauthenticated)

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
