{{- if .Values.agentController.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gastown.agentController.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gastown.agentController.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.agentController.replicaCount }}
  selector:
    matchLabels:
      {{- include "gastown.agentController.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gastown.agentController.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "gastown.agentController.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      containers:
        - name: agent-controller
          image: "{{ .Values.agentController.image.repository }}:{{ .Values.agentController.image.tag }}"
          imagePullPolicy: {{ .Values.agentController.image.pullPolicy }}
          env:
            - name: NAMESPACE
              value: {{ .Release.Namespace }}
            - name: BD_DAEMON_HOST
              value: {{ include "gastown.daemon.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
            - name: BD_DAEMON_PORT
              value: "{{ (index .Values "bd-daemon").daemon.tcp.port }}"
            - name: BD_DAEMON_HTTP_PORT
              value: "{{ (index .Values "bd-daemon").daemon.http.port }}"
            - name: BD_DAEMON_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "gastown.daemon.tokenSecretName" . }}
                  key: token
            - name: AGENT_IMAGE
              value: "{{ .Values.agentController.agentImage.repository }}:{{ .Values.agentController.agentImage.tag }}"
            {{- if .Values.agentController.apiKeySecret }}
            - name: API_KEY_SECRET
              value: {{ .Values.agentController.apiKeySecret }}
            {{- end }}
            {{- if .Values.agentController.coopImage }}
            - name: COOP_IMAGE
              value: {{ .Values.agentController.coopImage }}
            {{- end }}
            {{- if .Values.agentController.coopBuiltin }}
            - name: COOP_BUILTIN
              value: "true"
            {{- end }}
            {{- if .Values.agentController.credentialsSecret }}
            - name: CLAUDE_CREDENTIALS_SECRET
              value: {{ .Values.agentController.credentialsSecret }}
            {{- end }}
            {{- if .Values.agentController.daemonTokenSecret }}
            - name: DAEMON_TOKEN_SECRET
              value: {{ .Values.agentController.daemonTokenSecret }}
            {{- else }}
            - name: DAEMON_TOKEN_SECRET
              value: {{ include "gastown.daemon.tokenSecretName" . }}
            {{- end }}
            - name: GT_TOWN_NAME
              value: {{ .Values.agentController.townName | default .Release.Name }}
            {{- if .Values.agentController.watcherTransport }}
            - name: WATCHER_TRANSPORT
              value: {{ .Values.agentController.watcherTransport }}
            {{- end }}
            {{- if .Values.agentController.natsURL }}
            - name: NATS_URL
              value: {{ .Values.agentController.natsURL }}
            {{- else if (index .Values "bd-daemon" "nats" "enabled") }}
            - name: NATS_URL
              value: nats://{{ .Release.Name }}-bd-daemon-nats:4222
            {{- end }}
            {{- if .Values.agentController.natsTokenSecret }}
            - name: NATS_TOKEN_SECRET
              value: {{ .Values.agentController.natsTokenSecret }}
            {{- end }}
            {{- if .Values.agentController.gitCredentialsSecret }}
            - name: GIT_CREDENTIALS_SECRET
              value: {{ .Values.agentController.gitCredentialsSecret }}
            {{- end }}
            {{- if or .Values.agentController.coopBrokerURL .Values.coopBroker.enabled }}
            - name: COOP_BROKER_URL
              value: {{ .Values.agentController.coopBrokerURL | default (include "gastown.coopBroker.serviceURL" .) }}
            {{- end }}
            {{- if .Values.agentController.coopBrokerTokenSecret }}
            - name: COOP_BROKER_TOKEN_SECRET
              value: {{ .Values.agentController.coopBrokerTokenSecret }}
            {{- else if .Values.coopBroker.enabled }}
            - name: COOP_BROKER_TOKEN_SECRET
              value: {{ include "gastown.coopBroker.authTokenSecretName" . }}
            {{- end }}
            {{- if and .Values.coopBroker.enabled .Values.coopBroker.mux.enabled }}
            - name: COOP_MUX_URL
              value: {{ include "gastown.coopBroker.muxServiceURL" . }}
            {{- end }}
          resources:
            {{- toYaml .Values.agentController.resources | nindent 12 }}
      {{- with .Values.agentController.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agentController.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agentController.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
