feat: add integration framework and plugin architecture (fl-88c.1)

Add internal/integrations/ package with Integration interface (OnNewIssue,
OnResolved, OnRegression), factory registry, and dispatcher. Add
integrations_config table with CRUD queries. Add integration management
API endpoints. Wire IntegrationNotifier into notify.Dispatcher so
integrations fire alongside webhooks on all lifecycle events.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
