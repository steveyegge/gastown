feat: add comments and notifications API + dashboard (fl-4b0.9)

Add comment CRUD API endpoints (update, delete) alongside existing
list/create. Add notification API under /api/v1/notifications with
list, unread-count, mark-read, and mark-all-read endpoints.

Wire comment creation to insert LifecycleCommented lifecycle events.
Add comment thread section to issue detail page with @mention support.
Add notification bell with unread badge to nav bar, plus a dedicated
notifications page with mark-all-read. Add GetAccountByID for resolving
comment author names in the dashboard.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
