feat: add bulk operations (resolve, ignore, assign) for issues (fl-4b0.11)

- Add POST /api/{project_id}/bulk-issues/ endpoint supporting resolve,
  ignore, unresolve, and assign actions with lifecycle event creation
- Add BulkResolveIssues, BulkIgnoreIssues, BulkUnresolveIssues DB methods
- Add LifecycleIgnored and LifecycleAssigned lifecycle event types
- Add dashboard bulk handler with CSRF protection
- Add checkboxes to issue list with sticky bulk action bar
- Add select-all, resolve, ignore, unresolve, and assign-to-member UI
- Update lifecycle icon/label/class helpers for new event types
