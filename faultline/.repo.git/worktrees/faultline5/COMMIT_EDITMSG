feat: implement Docker event watcher goroutine (fl-csv)

Add internal/dockermon/ package that subscribes to Docker daemon event
stream and handles container lifecycle events:

- container.start: register labeled containers (faultline.monitor=true)
- container.die: critical alert on non-zero exit code
- container.oom: immediate critical with OOMKilled title
- container.stop: update last_seen timestamp
- container.health_status: track consecutive failures (1=warning, 3+=critical)

Maps containers to faultline projects via faultline.project label slug
lookup. State transitions fire into existing issue pipeline via
OnStateChange callback with platform:"docker" events.

Includes retry logic (60s) for Docker socket unavailability, in-memory
container cache with DB fallback, and SQLDBProvider for all persistence.

Wired into cmd/faultline/main.go following the dbmon pattern.
